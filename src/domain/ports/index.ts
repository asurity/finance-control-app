/**
 * Domain Ports barrel export
 * Interfaces agnósticas que definen contratos para la capa de infraestructura
 */

export type {
  IVoiceProvider,
  VoiceProviderState,
  FunctionCall,
  ToolDeclaration,
  VoiceSessionConfig,
  VoiceProviderType,
} from './IVoiceProvider';
