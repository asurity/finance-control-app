/**
 * @deprecated Tests para RealtimeClient (Fase 3)
 * Superseded por OpenAIRealtimeProvider.test.ts en FASE 7.
 * RealtimeClient es ahora un re-export de OpenAIRealtimeProvider.
 * Se eliminara junto con RealtimeClient.ts en FASE 8 (limpieza final).
 */

// eslint-disable-next-line jest/no-disabled-tests
describe.skip('RealtimeClient (deprecated - ver OpenAIRealtimeProvider.test.ts)', () => {
  it('placeholder', () => {});
});
