/**
 * Barrel export y registro de herramientas del Voice Agent
 * Fase 0: Setup inicial - Stub
 */

import { VoiceToolRegistry } from '../VoiceToolRegistry';

/**
 * Registra todas las herramientas disponibles en el registry
 * TODO: Implementar herramientas en Fase 2
 */
export function registerAllTools(): void {
  const registry = VoiceToolRegistry.getInstance();
  
  // TODO: Registrar herramientas en Fase 2
  // registry.register('create_expense', createExpenseTool);
  // registry.register('create_income', createIncomeTool);
  // etc.
}
